 每一个框是一个芯片/模块，框里的数字是引脚编号，箭头指向它连到哪。
> GND = 全部连一起，3.3V = 系统 3.3V，5V = 系统 5V，VBUS_PD = USB PD 协商后的电压(5~20V)

---

## 一、总览：哪些东西互相连着

```
                         ┌─────────────────────────┐
                         │      OLED 模块 (I2C)     │
                         │  VCC  GND  SCL  SDA     │
                         └────┬────┬────┬────┬──────┘
                              │    │    │    │
         ┌────────────────────┘    │    │    └──────────┐
         │                         │    │               │
         │   ┌─────────────────────┘    │               │
         │   │   ┌──────────────────────┘               │
         │   │   │                                      │
         ▼   ▼   ▼                                      ▼
    ┌─────────────────────────────────────────────────────────┐
    │                    I2C 总线 (SDA + SCL)                  │
    │           两根线，上挂 INA226 和 OLED 两个设备            │
    │            各有一个 10kΩ 上拉电阻到 3.3V                 │
    └────────┬───────────────┬────────────────────────────────┘
             │               │
             ▼               ▼
    ┌────────────┐    ┌──────────────┐
    │  INA226    │    │   STM32G030  │
    │ (MSOP-10)  │    │  (TSSOP-20)  │
    └────────────┘    └──────────────┘
                           │    │    │
              ┌────────────┘    │    └────────────┐
              ▼                 ▼                 ▼
         ┌────────┐     ┌──────────┐       ┌──────────┐
         │ CH224K │     │按键×3    │       │  LM358   │
         │(SOT23) │     │+LED      │       │ (SOIC-8) │
         └────────┘     └──────────┘       └──────────┘
                                               │
                                               ▼
                                         ┌──────────┐
                                         │ IRLZ44N  │
                                         │ MOSFET   │
                                         │ (TO-220) │
                                         └──────────┘
```

---

## 二、STM32G030F6P6（TSSOP-20）—— 所有引脚连接

```
                    ┌───────────────────────────────────────┐
                    │           STM32G030F6P6               │
                    │           TSSOP-20                    │
                    │                                       │
   INA226 SDA ─────┤1  PB7                          VDD 18├──── 3.3V
   OLED SDA        │                                VSS 17├──── GND
                    │                                       │
   INA226 SCL ─────┤2  PB6                         PA12 16├──── SWDIO (排针)
   OLED SCL        │                                PA11 15├──── SWCLK (排针)
                    │                                       │
   CH224K CFG2 ────┤3  PB1                          PB3 14├──── CH224K CFG3
                    │                                       │
   CH224K CFG1 ────┤4  PB0                          PA8 13├──── S8050 基极
                    │                                  (MOSFET 急停)
   按键 UP ────────┤5  PA0                           PA7 12├──── NTC + 上拉电阻
                    │                                       │
   按键 DOWN ──────┤6  PA1                           PA6 11├──── PWM → LM358 (经过 RC)
                    │                                       │
   按键 OK ────────┤7  PA2                           PA5 10├──── LED (经过 1kΩ)
                    │                                       │
   调试 UART_TX ───┤8  PA3                           PA4  9├──── 调试 UART_RX
                    │                                       │
                    └───────────────────────────────────────┘

引脚 19 (PA13) — 保留不接
引脚 20 (PA14) — OLED_RESET（接 OLED 模块的 RST 脚，模块无 RST 脚则不接）
```

### MCU 电源和调试详解

```
引脚 18 (VDD):
    │
    ├── 3.3V 电源
    ├── 100nF 电容 ── GND   ← 贴片电容，紧挨引脚 18 放
    ├── 100nF 电容 ── GND   ← 再放一个
    └── (如果有多个 VDD 脚，每个脚都配一个 100nF)

引脚 17 (VSS):
    └── GND（直接连地平面）

SWD 4P 排针（用来烧录程序）:
    排针 1 ── 3.3V
    排针 2 ── PA12 (SWDIO)
    排针 3 ── PA11 (SWCLK)
    排针 4 ── GND
```

---

## 三、CH224K（SOT-23-6）—— PD 触发芯片

```
                    ┌─────────────────────┐
                    │       CH224K        │
                    │     SOT-23-6        │
                    │                     │
       3.3V ───────┤1  VDD     VBUS  6├──┤
       C2 100nF    │                     │  ├── USB-C 的 VBUS 脚
       ── GND      │                     │  ├── C3 1μF ── GND
                    │                     │  └── VBUS_PD 网络（通往后面的电路）
       GND ────────┤2  GND     CFG3  5├──── PB3 (MCU 引脚 14)
                    │                     │
 MCU PB0 ──────────┤3  CFG1    CFG2  4├──── PB1 (MCU 引脚 3)
 (MCU 引脚 4)       │                     │
                    └─────────────────────┘
```

**CFG 引脚怎么控制（三态）：**

```
MCU 想要 HIGH:  GPIO 设为推挽输出，输出高电平
MCU 想要 LOW:   GPIO 设为推挽输出，输出低电平
MCU 想要 NC:    GPIO 设为浮空输入模式（不驱动，呈高阻态）

电压切换例子（设到 20V）:
  PB0(CFG1) = LOW   → 推挽输出低
  PB1(CFG2) = LOW   → 推挽输出低
  PB3(CFG3) = NC    → 浮空输入
  → CH224K 向充电器请求 20V
```

---

## 四、USB-C 16P 母座

```
              ┌───────────────────────────────┐
              │        USB-C 16P 母座          │
              │     (俯视图，引脚在底面)          │
              │                               │
              │  A1  GND  ──── GND            │
              │  A4  VBUS ──── VBUS_PD        │
              │  A5  CC1  ──── 悬空/不接       │
              │  A7  D+   ──── 不接            │
              │  A9  VBUS ──── VBUS_PD        │
              │  A12 GND  ──── GND            │
              │                               │
              │  B1  GND  ──── GND            │
              │  B4  VBUS ──── VBUS_PD        │
              │  B5  CC2  ──── 悬空/不接       │
              │  B7  D-   ──── 不接            │
              │  B9  VBUS ──── VBUS_PD        │
              │  B12 GND  ──── GND            │
              │                               │
              │  外壳固定脚 ──── GND            │
              └───────────────────────────────┘

总结: 4 个 VBUS 脚并在一起 → VBUS_PD 网络
      4 个 GND  脚并在一起 → GND
      其余脚全部不接
```

---

## 五、MP1584（SOIC-8）—— DCDC 降压

```
              ┌─────────────────────────────────┐
              │          MP1584                 │
              │         SOIC-8                  │
              │                                 │
   C6 100nF ──┤1  BST           SW   3├──┬── L1 22μH ──── 5V 输出
   (自举电容)  │                        │  │
              │                        │  └── D1(SS34) 阴极
   VBUS_PD ───┤2  VIN          GND  7├── GND     D1 阳极 ── GND
   C4 22μF    │                        │
   ── GND     │                        │
              │                        │
   VBUS_PD ───┤3  VIN           EN  6├── VBUS_PD (使能)
              │                        │
   VBUS_PD ───┤4  VIN           FB  5├──┬── R1 52.3kΩ ── 5V
   (各VIN脚   │                        │  │
    都接一起) │                        │  └── R2 10kΩ ── GND
              │                        │
              │   引脚 8 (SS) ── 悬空   │
              └─────────────────────────┘

5V 输出端:
  5V ── C5 22μF ── GND (输出滤波)
  5V ── 送往 AMS1117-3.3 输入
  5V ── 送往 LM358 引脚 8 (VCC)
```

---

## 六、AMS1117-3.3（SOT-223）—— 3.3V LDO

```
              ┌───────────────────┐
              │   AMS1117-3.3     │
              │    SOT-223        │
              │                   │
  5V ────────┤1  VIN    VOUT  2├── 3.3V 输出
  C7 10μF    │        ───────  ├── C8 10μF ── GND
  ── GND     │       TAB(GND)  └── C9 100nF ── GND
              │          │
  GND ────────┤3  GND    │
              │          │
              └──────────┴── GND (TAB 是大的散热焊盘，也要接 GND)

3.3V 输出去往:
  ├── STM32G030 引脚 18 (VDD)
  ├── CH224K 引脚 1 (VDD)
  ├── INA226 引脚 8 (VCC)
  ├── OLED 模块 VCC
  ├── I2C 上拉电阻 (R14, R15)
  ├── 按键上拉电阻 (R18, R19, R20)
  └── NTC 上拉电阻 (R22)
```

---

## 七、INA226（MSOP-10）—— 电流/电压检测

```
              ┌─────────────────────────────────┐
              │           INA226                │
              │          MSOP-10                │
              │                                 │
     GND ─────┤1  A0               VIN-  10├──┐
              │                             │  │
     GND ─────┤2  A1               VIN+   9├──┤ 这两根线是 Kelvin 检测线
              │                             │  │ 从采样电阻 R3 两端直接引出
  VBUS_PD ────┤3  VBUS             VCC    8├──┤ 3.3V
  C15 1μF     │                             │  └── C14 100nF ── GND
  ── GND      │                             │
              │                             │
     GND ─────┤4  GND              ALERT  7├── 悬空（不用）
              │                             │
  ──┬─────────┤5  SDA              SCL    6├──┬── MCU PB6 (I2C SCL)
    │         │                             │  │   OLED SCL
    │  R14    └─────────────────────────────┘  │
    │ 10kΩ                                     │  R15 10kΩ
    │                                          │
    └── 3.3V                                   └── 3.3V

A0=A1=GND → I2C 地址 = 0x40
```

---

## 八、LM358（SOIC-8）—— 恒流控制运放

```
              ┌──────────────────────────────────┐
              │            LM358                 │
              │           SOIC-8                 │
              │                                  │
  ── 反馈网络 ─┤1  OUT1              VCC   8├──── 5V
  (见下方详情)  │                                  │
              │                                  │
  ──┬─────────┤2  IN1-             OUT2   7├──── 悬空 (不用)
    │         │                                  │
    │R9 10kΩ  │                                  │
    │         │                                  │
    └── R3 高端(MOSFET S极)                      │
              │                                  │
  ──┬─────────┤3  IN1+             IN2-   6├──── GND (不用就接地)
    │         │                                  │
    │R6 10kΩ  │                                  │
    │         │                                  │
    └── V_SET (PWM设定值)        IN2+    5├──── GND
              │                                  │
     GND ─────┤4  GND                            │
              └──────────────────────────────────┘
```

### LM358 恒流环路的完整接法（展开引脚 1、2、3）：

```
                           PWM 设定电压的产生
═══════════════════════════════════════════════════════════════

MCU PA6 (引脚 11) ── 输出 PWM 方波 (0V/3.3V 跳变, 10kHz)
  │
  ├── R7 100kΩ ──┬──→ 这个点叫 V_SET (平滑后的直流电压, 0~70mV)
  │              │
  │              ├── R8 2.2kΩ ── GND    ← 分压: 3.3V × 2.2/(100+2.2) ≈ 70mV  max
  │              ├── C11 10μF ── GND     ← 滤波电容，把 PWM 变成平滑直流
  │              └── C13 10nF ── GND     ← 高频噪声滤除
  │
  │         V_SET 最终范围: 0 ~ 70mV
  │         对应电流:        0 ~ 3.5A  (70mV / 20mΩ = 3.5A)
  │


                          恒流闭环控制
═══════════════════════════════════════════════════════════════

                         LM358
                      ┌──────────┐
                      │          │
    V_SET ───┬────────┤3 IN1+    │
             │        │          │
             │        │          │
    R6 10kΩ  │        │          │
             │        │      1 OUT├──┬── R4 100Ω ── R5 1kΩ ── MOSFET G 极
    GND ─────┘        │          │  │
                      │          │  └── C10 100nF ──┐
                      │          │                  │
    MOSFET S极 ──R9───┤2 IN1-    │                  │
    (采样电阻高端)     │          │                  │
            10kΩ      │          │                  │
                      └──────────┘                  │
                                                    │
    ┌───────────────────────────────────────────────┘
    │  (C10 的另一端，形成积分反馈)
    │


                          功率回路
═══════════════════════════════════════════════════════════════

  VBUS_PD (来自 USB PD 的 5~20V)
    │
    │
    │   ┌───────────────────────┐
    │   │  IRLZ44N MOSFET       │
    │   │  TO-220               │
    │   │                       │
    └───┤D (漏极)               │
        │                       │
        │         G (栅极) ─────┼── 来自 LM358 OUT → R4 → R5
        │                       │
        │         S (源极) ─────┼──┬─── R3 (20mΩ 采样电阻) ─── GND
        └───────────────────────┘  │
                                   │
                                   ├── Kelvin 检测线 → INA226 引脚 9 (VIN+)
                                   ├── Kelvin 检测线 → LM358 引脚 2 (经过 R9)
                                   │
                                   │    (R3 另一端)
                                   ├──── Kelvin 检测线 → INA226 引脚 10 (VIN-)
                                   └──── GND (功率地，大电流)
```

---

## 九、OLED 模块（0.96" SSD1306 I2C）

```
              ┌─────────────────────┐
              │   OLED 模块          │
              │   4 脚  I2C 版       │
              │                     │
  3.3V ───────┤VCC                  │
  GND  ───────┤GND                  │
  MCU PB6 ────┤SCL                  │
  MCU PB7 ────┤SDA                  │
              │                     │
              │  (有的模块还有 RST 脚) │
   MCU PA14 ──┤RST  (没有就悬空)     │
              └─────────────────────┘
```

---

## 十、按键 ×3 + 状态 LED

```
  按键 UP (SW1):
      3.3V ── R18 10kΩ ──┬── MCU PA0 (引脚 5)
                         │
                         ├── 按键 ── GND
                         
  按键 DOWN (SW2):
      3.3V ── R19 10kΩ ──┬── MCU PA1 (引脚 6)
                         │
                         ├── 按键 ── GND
                         
  按键 OK (SW3):
      3.3V ── R20 10kΩ ──┬── MCU PA2 (引脚 7)
                         │
                         ├── 按键 ── GND

  工作原理:
    按键没按下 → 10kΩ 上拉到 3.3V → MCU 读到高电平
    按键按下   → 直接通 GND     → MCU 读到低电平


  状态 LED:
    MCU PA5 (引脚 10) ── R21 1kΩ ── LED 阳极
                                      │
                                    LED 阴极 ── GND
                                    
    PA5 输出高 → LED 亮
    PA5 输出低 → LED 灭
```

---

## 十一、可选扩展：NTC 温度检测 + 急停

```
  NTC 温度检测:
      3.3V ── R22 10kΩ (1%) ──┬── MCU PA7 (引脚 12, ADC 通道)
                              │
                              ├── C19 100nF ── GND (滤波)
                              │
                              └── NTC (10kΩ B=3950) ── GND
                                  
      NTC 贴紧 MOSFET 散热片安装，用导热胶固定


  MOSFET 急停 (硬件关断负载):
      MCU PA8 (引脚 13) ── R23 10kΩ ── S8050 基极 (B)
      
      S8050 集电极 (C) ──── MOSFET G 极
      S8050 发射极 (E) ──── GND
      
      PA8=高 → S8050 导通 → G 极拉到 GND → MOSFET 关断
      PA8=低 → S8050 截止 → G 极由 LM358 控制 → 正常工作
```

---

## 十二、完整连接速查表（按网络）

```
网络: GND
  连到: 所有芯片的 GND/VSS 脚、USB-C GND、去耦电容的 GND 端、
        采样电阻低端、按键低端、LED 阴极、SWD 排针
    
网络: 3.3V
  连到: STM32 VDD(18), CH224K VDD(1), INA226 VCC(8), OLED VCC,
        I2C 上拉电阻, 按键上拉电阻, NTC 上拉电阻

网络: 5V
  连到: LM358 VCC(8), AMS1117 输入

网络: VBUS_PD (5~20V)
  连到: USB-C VBUS 脚, CH224K VBUS(6), MP1584 VIN, MOSFET D 极,
        INA226 VBUS(3)

网络: I2C_SDA
  连到: MCU PB7(1), INA226 SDA(5), OLED SDA
        + 上拉电阻 R14 10kΩ → 3.3V

网络: I2C_SCL
  连到: MCU PB6(2), INA226 SCL(6), OLED SCL
        + 上拉电阻 R15 10kΩ → 3.3V
